# Cloudflare Pages SPA fallback: serve index.html for any path that is not a
# real file, so deep links and a stale PWA start URL resolve instead of 404ing.
# GitHub Pages ignores this file, so it is safe to ship in both builds.
/*    /index.html   200
